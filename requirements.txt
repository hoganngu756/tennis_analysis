# Tennis Analysis System — Python 3.11
# Install:  python3.11 -m venv .venv && source .venv/bin/activate
#           pip install -r requirements.txt

# Core CV / numerics
numpy>=1.26,<2.2
opencv-python>=4.9
pandas>=2.1
scipy>=1.11

# Detection, tracking and pose (ByteTrack ships inside ultralytics)
ultralytics>=8.3.0
torch>=2.2
torchvision>=0.17
# ByteTrack's assignment solver. Ultralytics auto-installs this on first use; pinning
# it here avoids a surprise pip install partway through the first run.
lap>=0.5.12

# Stroke classification
scikit-learn>=1.4
joblib>=1.3

# Config
PyYAML>=6.0

# Dashboard
streamlit>=1.31
altair>=5.2

# Dataset acquisition (see scripts/download_datasets.py)
roboflow>=1.1
requests>=2.31
tqdm>=4.66

# Tests
pytest>=8.0
